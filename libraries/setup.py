from setuptools import setup, find_packages

setup(
    name="libraries",
    version="0.1",
    packages=find_packages(),
    author_email="Melanie.Trelet@bfr.bund.de",
    url="https://github.com/KIDA-BfR/GenEval.git"
    #don't know if it's the right access path
)
